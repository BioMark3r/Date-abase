# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
venv/
.venv/
env/
.env

# Database (local dev only — data volume is used in Docker)
data/
*.db
*.sqlite
*.sqlite3

# Environment / secrets
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Docker
.dockerignore
# Per-host overrides (e.g. the Pi's real Tailscale APP_DOMAIN/APP_ORIGIN).
# Compose auto-merges this over docker-compose.yml; keep it out of git so a
# pull can never reset your production WebAuthn/Face ID domain.
docker-compose.override.yml
